{
  "homepage": "https://fictiveUniverse.github.io/",
  "scripts":{
    "predeploy": "yarn run build",
    "deploy": "gh-pages -b master -d build"
  },
  "dependencies": {
    "gh-pages": "^3.2.0"
  }
}
